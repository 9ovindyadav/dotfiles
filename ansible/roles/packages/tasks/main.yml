- name: Update apt cache
  become: true
  apt:
    update_cache: true
    cache_valid_time: 3600

- name: Install packages
  become: true
  apt:
    name:
      - git
      - stow
      - vim
      - tmux
      - curl
      - wget
      - xclip
      - fzf
      - build-essential
      - unzip
      - podman
    state: present
